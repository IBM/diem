.diem--content
    .leftNavWrapper
        ibm-sidenav(rail="true",[expanded]="true")

            ng-container(*ngFor="let item of items")

                ng-template([ngIf]="! item.section")

                    ng-template([ngIf]="item.name && item.location")

                        ibm-sidenav-item(
                            [route]="[item.name]",
                            [active]= "item.title === doc_title",
                            [title]= "item.title"
                            )

                            | {{item.title}}

                    ng-template([ngIf]="item.title==='divider'")

                            hr.LeftNav-module--divider--1Z49I

                    ng-template([ngIf]="! item.name && item.location")

                        li.cds--side-nav__item

                            a.cds--side-nav__link.LeftNav-module--outboundLink--13-_V([attr.href]="item.location", target="_blank", rel="noreferrer")
                                .cds--side-nav__icon.cds--side-nav__icon--small
                                    svg(appIcon='launch', size='20')
                                span.cds--side-nav__link-text {{item.title}}

                ng-template([ngIf]="item.section")

                    ibm-sidenav-menu([title]="item.section.title")

                        ng-container(*ngFor="let item of item.section.items")

                            ibm-sidenav-item(
                                [route]="[item.name]",
                                [active]= "item.title === doc_title"
                                )

                                 | {{item.title}}

    .container
        .ex-doc-header([ngStyle]="header")
            .cds--grid
                .cds--row
                    .cds--col-lg-12
                        h1.PageHeader-module--text--1cbUz([innerHTML]="doc_title")
        .cds--grid.Main-module--padded--_Y6lG
            section.section([innerHTML]="doc_content")