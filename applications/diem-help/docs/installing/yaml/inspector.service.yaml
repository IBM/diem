kind: Service
apiVersion: v1
metadata:
  name: inspector
  namespace: default
  labels:
    app: inspector
spec:
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
  selector:
    app: inspector
  type: ClusterIP
  sessionAffinity: None
status:
  loadBalancer: {}
